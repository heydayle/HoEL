/**
 * The localStorage key used to persist the authenticated user's data.
 * This key is derived from the Supabase project reference and is used
 * by {@link AuthSyncProvider} and {@link getUserLocal} to store/read
 * user information synchronously on the client.
 */
export const AUTH_TOKEN_KEY = 'sb-hpnokwlodebafzgebopj-auth-token';
